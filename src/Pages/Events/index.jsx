import React from 'react';

// Components renderizados na página que lista eventos retornados pela API
import EventsComponent from '../../Components/Events';

function Events() {
  return (
    <EventsComponent />
  )
};

export default Events;
